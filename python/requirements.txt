# Core: binding build + gym env + comparison plots
pybind11>=2.10
numpy
gymnasium
matplotlib

# Optional: the deep-RL examples (train_dqn.py, train_ppo.py)
torch
tensorboard
stable-baselines3
sb3-contrib
